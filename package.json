{
  "name": "open-fresh",
  "packageManager": "yarn@3.6.4",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "yarn workspace client dev",
    "server": "yarn workspace server dev",
    "plan": "yarn workspace cli plan",
    "order": "yarn workspace cli order"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "modelfusion": "^0.35.2",
    "zod": "^3.21.4",
    "zod-to-json-schema": "^3.21.4"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
